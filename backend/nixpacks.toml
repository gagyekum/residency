# Nixpacks configuration for Railway
# Let nixpacks auto-detect Python, just override start command

[start]
cmd = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && (python manage.py createsuperuser --noinput || true) && gunicorn config.wsgi:application --config gunicorn.conf.py"
