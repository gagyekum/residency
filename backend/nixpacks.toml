# Nixpacks configuration for Railway
# Let nixpacks auto-detect Python, just override start command

[start]
cmd = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py ensure_superuser && gunicorn config.wsgi:application --config gunicorn.conf.py"
